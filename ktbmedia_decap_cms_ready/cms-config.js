/**
 * KTB Media CMS Configuration
 *
 * TO JEST JEDYNY PLIK KTÓRY MUSISZ EDYTOWAĆ ŻEBY PODŁĄCZYĆ SUPABASE.
 *
 * Po założeniu projektu na supabase.com wklej poniżej:
 * - SUPABASE_URL — Settings → API → Project URL
 * - SUPABASE_ANON_KEY — Settings → API → anon public
 *
 * Jeśli zostawisz puste, strona będzie używać lokalnego content.js (bez CMS).
 */
window.KTB_CMS_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
